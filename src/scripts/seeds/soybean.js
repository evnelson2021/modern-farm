export const createSoybean = ()=>{
    const soybeanObj = {
        type: "Soybean",
        height: 20,
        output: 4
    }
return soybeanObj
}

// create and export function to make soybean object